FutureDr.initTimeline=()=>{gsap.utils.toArray('.milestone').forEach(card=>{card.addEventListener('pointermove',event=>{const rect=card.getBoundingClientRect();const x=(event.clientX-rect.left)/rect.width-.5;const y=(event.clientY-rect.top)/rect.height-.5;gsap.to(card,{rotationY:x*5,rotationX:-y*5,duration:.35,ease:'power2.out'})});card.addEventListener('pointerleave',()=>gsap.to(card,{rotationY:0,rotationX:0,duration:.5,ease:'power2.out'}))})};
