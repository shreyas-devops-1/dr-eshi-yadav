FutureDr.initMemories=()=>{gsap.utils.toArray('.memory-card').forEach(card=>{card.addEventListener('pointermove',event=>{const rect=card.getBoundingClientRect();const x=(event.clientX-rect.left)/rect.width-.5;const y=(event.clientY-rect.top)/rect.height-.5;gsap.to(card,{rotationY:x*8,rotationX:-y*8,y:-4,duration:.35,ease:'power2.out'})});card.addEventListener('pointerleave',()=>gsap.to(card,{rotationY:0,rotationX:0,y:0,duration:.55,ease:'power2.out'}))})};
